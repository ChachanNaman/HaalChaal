NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only (no NEXT_PUBLIC_ prefix -- never sent to the browser). Must match the backend's
# PUBLIC_BASE_URL and INTERNAL_API_KEY.
BACKEND_URL=
BACKEND_INTERNAL_API_KEY=

# Used by /api/translate to render transcripts in English on demand
GROQ_API_KEY=
